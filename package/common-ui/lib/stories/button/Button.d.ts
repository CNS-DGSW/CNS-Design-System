/// <reference types="react" />
import { ButtonProps } from './button.type';
export declare const Button: ({ size, color, clickEvent, hover, border, radius, disable, shadow, children, }: ButtonProps) => JSX.Element;
