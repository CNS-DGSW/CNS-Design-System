import { ColorType } from './types';

export const lightThemeToken: ColorType = Object.freeze({
    black: '#000',
    green: 'green',
    white: '#fff',
    blue: 'blue',
    red: 'red',
});

export const darkThemeToken: ColorType = Object.freeze({
    black: '#fff',
    green: 'green',
    white: '#000',
    blue: 'blue',
    red: 'red',
});
